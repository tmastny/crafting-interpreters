fun find(a) {
  for (var b = 1; a < 10000; b = temp + b) {
    if (a == b) {
      return "found!";
    }
  }

  return "not found :(";
}

print find(1);
print find(10001);

// fun find(a) { for (var b = 1; a < 10000; b = temp + b) { if (a == b) {return "found!";}} return "not found :(";} print find(1);

fun fib(n) {
  if (n <= 1) return n;
  return fib(n - 2) + fib(n - 1);
}

for (var i = 0; i < 20; i = i + 1) {
  print fib(i);
}
