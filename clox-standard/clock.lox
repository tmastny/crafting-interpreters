// clox: 3.204
// jlox: 8.223

// comment out debug flags in common.h
fun fib(n) {
  if (n < 2) return n;
  return fib(n - 2) + fib(n - 1);
}

var start = clock();
print fib(35);
print clock() - start;
